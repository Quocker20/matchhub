package com.quoc.matchhub.backend.enums;

public enum TournamentRole {
    OWNER,   // Chủ giải (Full quyền)
    REFEREE, // Trọng tài (Quyền cập nhật tỷ số)
    STAFF    // Thư ký/Nhân viên (Quyền check-in)
}